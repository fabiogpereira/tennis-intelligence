# Data schemas

Versioned schemas for match, point, player, source manifest, and model output belong here. The minimum point-level proposal is documented in [research/datasets.md](../../research/datasets.md). No schema is frozen until a source snapshot and parser tests exist.
